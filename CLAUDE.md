# CLAUDE.md

Zenn（https://zenn.dev/zeroyen_dev）の記事を管理するリポジトリ。
`articles/*.md` を push すると Zenn に自動反映される。運用ルールは `README.md` が正本。

## モデル選択とクレジット運用

**原則: 単純作業は軽量モデル、調査・分析は上位モデル。**

| 作業の種類 | 具体例 | 使うもの |
| --- | --- | --- |
| 機械作業（判断がほぼ不要） | 誤字修正、frontmatter の書き換え、ファイル名/スラッグ変更、リンク切れ洗い出し、記事一覧の集計、`git add/commit/push` | `quick` サブエージェント（Haiku） |
| 執筆・推敲（判断あり） | 本文の書き直し、構成の並べ替え、コードサンプルの検証 | メインセッションのまま |
| 調査・分析（網羅性が必要） | 記事テーマ選定、キーワード調査、Zenn 内の競合記事分析、技術的事実の裏取り、収益導線の設計 | `research` サブエージェント（Opus / effort high） |

### 委譲するかの判断基準

サブエージェントは**別コンテキストで起動する**ため、起動時に前提を読み直すコストがかかる。
そのため次で線を引く。

- **委譲しない**: 1〜2 手で終わる作業（1 ファイルの 1 行修正、`git status` の確認など）。
  委譲コストのほうが高くつくので、メインセッションで直接やる。
- **委譲する**: 3 手以上ある定型作業（複数記事の一括修正、全記事の frontmatter 点検など）→ `quick`。
- **委譲する**: 調べ物が主体で、往復が多く読む量が多い作業 → `research`。

### 実行方法

```
# 軽量モデルに投げる
Agent(subagent_type: "quick", prompt: "...")

# 調査に投げる
Agent(subagent_type: "research", prompt: "...")
```

セッション全体のモデルを切り替えるときは `/model`、思考量は `/effort` で変える。

### 注意

- 上の表に沿った委譲は、このファイルによって明示的に許可されている。ユーザーへの再確認は不要。
- ただし **push・公開・削除** を伴う操作は、モデルに関係なくメインセッションで実行内容を確認してから行う。
- 記事の内容そのもの（数字の裏取り、宣伝ルール）は `README.md` の「決めていること」に従う。軽量モデルに投げる場合もこの制約をプロンプトに明記する。

## 記事の追加

原稿の正本は別リポジトリ（`jibun-kaizoudo/content/zenn/`）。
コピーして `published: true` にしてから push する。詳細は `README.md`。

## 回答の最後に推奨モデルを書く

**すべての回答の一番最後**（確度の自己評価よりさらに後）に、その作業に最適なモデルを1行で書く。
ユーザーが次に同じ作業を頼むとき、どのモデルに切り替えるべきか即判断できるようにするため。

### 書式

```
> **推奨モデル: Haiku 4.5** ｜ 判断がほぼ不要な定型修正のため。
```

- モデル名と理由（40字以内）だけ。前置きも解説も付けない。
- 理由は「なぜそのモデルで足りるか / なぜ上位が要るか」に絞る。
- 現在のモデルと推奨が違う場合のみ、理由の後ろに `→ /model で切り替え推奨` を足す。

### 選び方

| 推奨モデル | 該当する作業 |
| --- | --- |
| **Haiku 4.5** | 誤字修正、frontmatter 編集、ファイル操作、一括置換、集計、git 操作、単純な質問への回答。**結果をその場で検証できる短い作業に限る** |
| **Sonnet 5** | 本文の推敲、構成の並べ替え、コードの読み書き、手順書の作成、一般的な相談 |
| **Opus 5** | 既定。市場調査、競合分析、キーワード設計、収益導線の設計、法令・税務の確認、複数案の比較検討、原因不明の不具合調査 |
| **Fable 5.1** | Opus 5 を effort high で回しても足りなかったとき。高難度の推論、長時間の調査ループ |

### 判断の順番（重要）

**モデルを下げる前に、まず effort を下げる。** これが公式の推奨順で、いちばん安く試せる。

1. `/effort low` または `medium` を試す
2. それでも過剰なら、モデルを1段下げる
3. 逆に品質が足りないなら、モデルを上げるより先に effort を上げる

### 「トークン単価」で判断しない

比較する単位は **完了1件あたりのコスト**。上位モデルは往復回数・読み直し・手戻りが減るため、
単価が高くても総額で安くなることがある。公式の実測では、上位モデルを低 effort で回したほうが、
下位モデルを既定 effort で回すより正答率が高く、1件あたりのコストも安いケースが報告されている。

そのため **Haiku を長い作業に投げない**。やり直しが発生した時点で損になる。
Haiku が向くのは「量が多く、出力の正しさをその場で確認できる」作業。

### 参照した一次情報（確認日 2026-09-04）

- モデル比較・価格: https://platform.claude.com/docs/en/about-claude/models/overview
- コスト最適化の考え方: https://platform.claude.com/docs/en/about-claude/models/optimizing-for-cost-and-intelligence

価格（$/1M トークン、入力/出力）: Fable 5.1 = 10/50、Opus 5 = 5/25、Sonnet 5 = 2/10、Haiku 4.5 = 1/5。
キャッシュ読み込みは通常 0.1 倍だが、Fable 5.1 のみ 0.025 倍。
同じ文脈を何度も読み直す長いループでは、この差が効いて上位モデルが不利にならないことがある。

注意: 上記は API 従量課金の価格。Claude Code のサブスクリプション枠（クレジット）の
消費係数と一致するかは未確認のため、金額そのものではなく**相対的な使い分けの根拠**として扱う。
